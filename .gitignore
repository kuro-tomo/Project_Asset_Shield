# =================================================================
# Asset Shield - Master Genome v1.0 (.gitignore)
# =================================================================

# -----------------------------------------------------------------
# 1. ⚡️ SECURITY & CREDENTIALS (最優先：絶対に漏洩させてはならない)
# -----------------------------------------------------------------
.env
.env.*
*.key
*.token
*.pem
*.cert
config/credentials.env
secrets/
auth/

# -----------------------------------------------------------------
# 2. 📊 DATA & BACKTEST (20年分のデータや巨大なバイナリ)
# -----------------------------------------------------------------
# J-Quants 20年分の生データやキャッシュは Git に含めない
data/
*.csv
*.parquet
*.h5
*.feather
*.sqlite3
*.db
!/docs/*.db  # 憲章等で必要な空のDB定義があれば除外
logs/*.db
logs/*.db-shm
logs/*.db-wal

# -----------------------------------------------------------------
# 3. 🐍 PYTHON & ENVIRONMENT (開発環境のノイズ)
# -----------------------------------------------------------------
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
.venv/
build/
dist/
*.egg-info/
.mypy_cache/
.pytest_cache/
.ruff_cache/

# -----------------------------------------------------------------
# 4. 🍎 MACOS & IDE (M4 Pro / MacBook Air 環境特有)
# -----------------------------------------------------------------
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes

# VS Code / Roo Code
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
.history/

# JetBrains
.idea/

# -----------------------------------------------------------------
# 5. 🤖 AI AGENT & OUTPUTS (Roo Code / Claude Code 生成物)
# -----------------------------------------------------------------
# 実戦運用（Backtest）の巨大なレポート出力
output/reports/
mock_*.log
sovereign_audit.log
.owner_pulse

# -----------------------------------------------------------------
# 6. 🗄️ ARCHIVE (過去の遺産：既に整理済みの場合は除外)
# -----------------------------------------------------------------
# 既に /archive へ移動済みの「古い残骸」を Git に含めたくない場合は
# 以下のコメントアウトを解除してください
# archive/